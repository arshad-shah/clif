---
"@arshad-shah/clif": patch
---

Bring the published package README up to date with the current feature set:
list `createTaskList` under Async UI, add the `@arshad-shah/clif/banner`
FIGfont engine to "What's included" and the tree-shakeable entry points, and
note the chainable `style` / `gradient` / `link` color APIs. Docs-only — no
runtime changes.
